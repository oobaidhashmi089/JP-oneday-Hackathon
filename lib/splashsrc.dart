import 'package:flutter/material.dart';

class Splashsrc extends StatelessWidget {
  const Splashsrc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
